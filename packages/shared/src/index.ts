/**
 * WU0 placeholder. WU3+ will export Zod DTOs for:
 *   - auth (LoginRequest, SessionResponse, Role)
 *   - marbete (Create, Update, Assign, List, Counters, MaskedCode)
 *   - dispositivo (Create, Update, Revoke, List)
 *   - audit (Entry, ListFilter, Action enum)
 *   - common errors (AppErrorCode union, ApiErrorEnvelope)
 *
 * Keeping this file empty (apart from the comment) keeps WU0 reviewable
 * and lets each subsequent WU own its delta without merge noise.
 */
export const BACKOFFICE_VERSION = '0.1.0';
