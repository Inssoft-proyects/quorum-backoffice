# Web environment (copy to .env.local). NEVER commit.

# API URL the browser hits.
# In production behind nginx reverse proxy at quorum.asistentepro.mx,
# the browser calls the same origin (no CORS).
NEXT_PUBLIC_API_URL=https://quorum.asistentepro.mx