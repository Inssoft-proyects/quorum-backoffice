# Web environment (copy to .env.local). NEVER commit.

# DEV: API server runs on port 3100 (3xx = dev, 4xx = prod convention)
# PROD: change to http://127.0.0.1:4100
NEXT_PUBLIC_API_URL=http://127.0.0.1:3100